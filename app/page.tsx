export default function HomePage() {
	return (
		<div className="min-h-dvh w-full flex">
			Go to <code className="underline px-3">/dex/species/[id]</code> to
			see a Pokemon species.
		</div>
	);
}
